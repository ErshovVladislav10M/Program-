#include <iostream>
#include <vector>
#include <cmath>
using namespace std;
void print_palindrom(int n){
 vector <int> a(n/2);
 for (int i = pow(10,n/2); i < pow(10,n/2+1); i++)
  if((i*2 - i%10)%9 == 0){
   int c = i/10;
   for (int l = 0; l < n/2; l++){
    a[l] = c%10;
    c /= 10;
   }
   int k = 0;
   int b = 0;
   int d = 0;
   for (int l = n/2-1; l >= 0; l--){
    k += a[l]*pow(10,b);
    b++;
   }
   while (a[d] == 0)
    d++;
   cout << i;
   for (int j = 0; j < d; j++)
    cout << 0;
   cout << k << endl;
  }
}
int main(){
 int n;
 cin >> n;
 print_palindrom(n);
 return 0;
}
