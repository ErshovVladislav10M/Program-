#include <iostream>
#include <cmath>
using namespace std;
int Migraine_minimization(int N, int k){
 int n = sqrt(N);
 N -= n*n;
 k++;
 if (N==0)
  return k;
 return Migraine_minimization(N, k);
}
int main(){
 int N;
 cin >> N;
 cout << 4*Migraine_minimization(N, 0);
 return 0;
}
