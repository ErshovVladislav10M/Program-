#include <iostream>
#include <cmath>
using namespace std;
int Migraine_minimization(int N){
 int n = sqrt(N);
 N -= n*n;
 if (N==0)
  return 1;
 return 1 + Migraine_minimization(N);
}
int main(){
 int N;
 cin >> N;
 cout << 4*Migraine_minimization(N);
 return 0;
}
