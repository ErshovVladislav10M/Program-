#include <iostream>
#include <queue>
using namespace std;
int main(){
 int n;
 cin >> n;
 int* A[n];
 int* T[n];
 int* S[n];
 for(int i = 0; i < n; i++)
  cin >> A[i] >> T[i];
 for(int i = 0; i < n; i++)
  for(int l = i; l < n; l++)
   if(T[i] > T[l]){
    swap(A[i], A[l]);
    swap(T[i], T[l]);
    swap(S[i], S[i])
   }
 while
 return 0;
}
