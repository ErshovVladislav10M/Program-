#include <iostream> 
#include <vector>
using namespace std; 
int main() { 
 int m; 
 double x; 
 cin >> x; 
 cin >> m; 
 int b = m; 
 vector <double> Array(m); 
 for (int i = 0; i < m; i++) 
  cin >> Array[i]; 
 while (x != Array[b/2]) { 
  if (x > Array[b/2]) 
   b /= 2; 
  else b += b/2; 
 } 
 if (x == Array[b/2]) 
  cout << "Есть"; 
 else cout << "Нету"; 
 return 0; 
}
