#include <iostream>
using namespace std;

int main() {
	int n;
	| //побитовое сложение
	& //побитовое умножение
	~ //битовое не
	^ //побитовое исключающее или
	cin >> n;
	cout << (1 << (n));//cдвиг на n разрядов влево
	cout << ((n >> i) & 1);//побитовая операция и
	system("pause");
	return 0;
}
