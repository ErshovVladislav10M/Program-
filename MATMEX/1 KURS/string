#include <iostream>
#include <string>
using namespace std;

int main() {
	string s;
	getline(cin, s); //ввод строки до enter
	cin.get(); //удаление очередного символа: в данном случае enter
	string s1 = "qwerty", s2;
	cout << s1.size() << endl; //размер
	cout << s1.empty() << endl; //проверка на пустоту
	const char* A = s1.c_str(); //присвоение типу const char
	cout << A << endl;
	cout << s1[0] << endl; //первый элемент
	cout << s1.at(1) << endl; //второй элемент
	cout << s1.substr(1, 3) << endl; //со второго три элемента
	system("pause");
	return 0;
}
