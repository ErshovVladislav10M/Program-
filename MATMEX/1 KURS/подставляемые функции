#include <iostream>
using namespace std;

inline void f(int n) { //работает как #define. только для всего тела функции
	cout << n * 2;
}
int main() {
	int n;
	cin >> n;
	f(n);
	system("pause");
	return 0;
}
