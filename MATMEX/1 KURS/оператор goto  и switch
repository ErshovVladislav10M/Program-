#include <iostream>
using namespace std;

int main() {
	for (int i = 0; i < 20; i++) {
		cout << i << endl;
		if (i > 13)
			goto f;
	}
f: cout << "end";
        int n;
	cin >> n;
        switch(n % 2){
	case 1:{ cout << 1; break; }
	case 0:{ cout << 2; break; }
	default: cout << "error";
	}
	system("pause");
	return 0;
}
