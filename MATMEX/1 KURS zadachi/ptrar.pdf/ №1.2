#include <iostream>
using namespace std;

int f(int *A, int size, int t) {
	int q = 0;
	for (int i = 0; i < 10; i++) {
		q += A[t];
		q %= size;
	}
	return A[q];
}

int main() {
	int A[7];
	for (int i = 0; i < 7; i++)
		A[i] = i + 1;
	cout << f(A, 7, 4) << endl;
	system("pause");
	return 0;
}
