#include <iostream>
#include <map>
using namespace std;

int main() {
	map<int, int> M;
	int n, p, q;
	cin >> n;
	for (int i = 0; i < n; i++) {
		cin >> p >> q;
		M[p] = q;
	}
	cin >> n;
	for (int i = 0; i < n; i++) {
		cin >> p;
		if (M.count(p)) cout << M[p];
		else cout << p;
	}
	system("pause");
	return 0;
}
