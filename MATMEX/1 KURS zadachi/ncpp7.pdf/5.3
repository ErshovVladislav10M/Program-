#include <iostream>
#include <set>
using namespace std;

int main() {
	set<int> S, C, V;
	int n = 1;
	while (n != 0) {
		cin >> n;
		if (C.count(n))
			V.insert(n);
		if (S.count(n))
			C.insert(n);
		S.insert(n);
	}
	for (auto x : V) C.erase(x);
	for (auto x : C) cout << x << " ";
	system("pause");
	return 0;
}
