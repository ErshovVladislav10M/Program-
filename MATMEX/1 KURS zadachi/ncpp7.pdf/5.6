#include <iostream>
#include <map>
using namespace std;

int main() {
	map<int, int> M;
	int n, p, q;
	cin >> n;
	for (int i = 0; i < n; i++) {
		cin >> p >> q;
		M[p] = q;
	}
	for (auto x : M) cout << x.first << " (" << x.second << ")" << endl;
	system("pause");
	return 0;
}
