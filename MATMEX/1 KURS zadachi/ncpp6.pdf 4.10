#include <iostream>
#include <fstream>
#include <sstream>
using namespace std;


int main(){
	string str = "1.0 2.4 6.9 11.1";
	stringstream st(str);
	double n, sum = 0;
	while(st >> n) sum += n;
	cout << sum << endl;
	return 0;
}
