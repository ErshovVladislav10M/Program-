#include <iostream>

using namespace std;

template<class T>
struct Node
{
	T d;
	Node<T> *n;
	Node(int dd, Node<T> *nn = nullptr) : d(dd), n(nn) {}
};

template<class T>
Node<T> * length(Node<T> *f)
{
	Node *p = f;
	return p->n;
}

int main()
{

	system("pause");
	return 0;
}
