#include <iostream>

using namespace std;

template<class T>
struct Node
{
	T d;
	Node<T> *n;
	bool j;
	Node(int dd = 0, Node<T> *nn = nullptr) : d(dd), n(nn) {}
};

template<class T>
void add_last(Node<T> *f, int d)
{
	while (f != nullptr)
	{
		f = f->n;
	}
	Node<T> *p = new Node<T>(d);
	f->p;
}


int main()
{

	system("pause");
	return 0;
}
