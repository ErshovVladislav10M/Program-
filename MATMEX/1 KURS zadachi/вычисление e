#include <iostream>
#include <cmath>
using namespace std;

int main() {
	double n = 2;
	double x = 1;
	while (fabs(x - exp(1)) / exp(1) > 0.01){
		x = pow((1 + 1 / n), n);
		n++;
	}
	cout << n << endl;
	system("pause");
	return 0;
}
