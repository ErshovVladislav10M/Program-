Ершов Владислав Евгеньевич
Решить следующие задачи параллельно при помощи OpenMP (если другое не указано в условии задачи, то двумя
потоками; очевидно неэффективные, даже параллельные, решения не принимаются).
1. Написать функцию, принимающую массив из миллиона двумерных векторов и поворачивающую их всех на угол
π/3, т. е. домножающую каждый из них на матрицу 

cos φ − sin φ
sin φ cos φ

, где φ = π/3.

2. Преобразование матрицы 50 × 50 состоит в следующем: к каждому элементу добавляется сумма элементов
его строки и столбца, после чего полученная сумма делится на 100. Написать функцию, применяющую указанное
преобразование к матрице, переданной в качестве параметра, 25 раз.
3. Написать функцию, принимающую строку из 10000 символов и вычисляющую сумму записанных в этой строке
чисел. Можно предполагать, что в этой строке записаны неотрицательные числа из небольшого числа цифр (так,
чтобы все промежуточные и окончательный результаты влезали в unsigned long long int), разделенные пробелами.
