1. Написать функцию rl с параметром-указателем на корень дерева, возвращающую указатель на самый правый
лист.

2. Написать функцию delLeftmostLeaf с параметром-указателем на корень дерева, удаляющий самый левый лист.

3. Написать функцию first_d с параметром-указателем на корень дерева и параметром d типа T и возвращающий
указатель на первого сына корня с данными d. Если такого узла нет, возвратить нулевой указатель.

4. Написать функцию addAGL с параметром-указателем на корень дерева, добавляющую новый узел с данными 0
на место единственного сына самого левого узла.

5. Написать функцию search2 с параметром-указателем на корень дерева и параметрами d1 и d2 типа T, и возвра-
щающий указатель на узел с данными d1 и левым сыном, данные которого равны d2. Если таких узлов нет, возвратить
нулевой указатель.
