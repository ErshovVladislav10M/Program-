1. Добавить в класс SList <T> метод fifth, принимающий данные и возвращающий указатель на пятый узел списка
с указанными данными, если такой узел в списке есть, а иначе возбуждающий исключение NoSuchEntry.
2. Добавить в класс SList <T> метод insert_after, добавляющий элемент с указанными данными, сразу после
первого, удовлетворяющего заданному условию. Единственным параметром этого метода будет функция типа bool (T),
где T — шаблонный параметр. При этом, функция-параметр может возбуждать исключение типа CheckFailed. Если
это происходит на хотя бы одном элементе списка, список должен остаться неизменным.
3. Добавить в класс DList <T> метод find6X7Y, принимающий два параметра x и y типа T и возвращающий
указатель на узел, содержащий шестой с конца x перед седьмым y.
4. Добавить в класс DList <T> метод remove_surr c параметром p, тип которого — указатель на узел списка, и
удаляющий элемент, на который указывает p, вместе с предыдущим и последующим, если такие есть.
