#include <iostream>
#include <string>
#include <list>

using namespace std;

struct Object
{
	virtual ~Object() {}
};

struct Integer : Object
{
	int data;
	Integer(int a) :data(a) {}
};

struct Double : Object
{
	double data;
	Double(double a) :data(a) {}
};

struct String : Object
{
	string data;
	String(string a) :data(a) {}
};

struct List : Object
{
	list<Object> data;
	List(list<Object> a) { data = a; }
};

int main()
{

	system("pause");
	return 0;
}
