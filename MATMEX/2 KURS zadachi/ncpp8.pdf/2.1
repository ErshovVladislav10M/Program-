#include <iostream>
#include <set>

using namespace std;

set <int> A;
int col = 1;

struct Foo
{
	int n;
	Foo() { n = col; A.insert(col); col++; }
	~Foo() { A.erase(n); cout << n << endl; }
};

Foo a, B[3];

void f()
{
	cout << endl;
	{static Foo b;}
	Foo c;
	Foo C[2];
}

int main()
{
	for (auto x : A) cout << x << " ";
	cout << endl;
	f();
	for (auto x : A) cout << x << " ";
	cout << endl;
	system("pause");
	return 0;
}
