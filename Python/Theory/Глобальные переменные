#создание локальной переменной
t = 1

#неправильное использование
def f():
    t = 2 #это не глобальна t, а созданная в этой функции локальная
    
#чтобы избежать этого
def f():
    global t #принудительное использование глобальной переменной
    t = 2
