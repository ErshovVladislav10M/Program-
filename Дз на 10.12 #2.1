#include <iostream>
#include <vector>
using namespace std;
 int main(){
   int n;
   int b = 0;
   cin >> n;
   vector <double> Array(n);
   vector <double> Array1(n);
   for (int i = 0; i < n; i++)
    cin >> Array[i];
   for (int l = 0; l < n; l++){
    for (int i = 0; i < n; i++)
     if (Array[l] < Array[i])
      b++;
    Array1[b] = Array[l];
    b = 0;
   }
   for (int i = 0; i < n; i++)
    cout << Array1[i] << " ";
   return 0;
 }
