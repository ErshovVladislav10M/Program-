#include <iostream>
#include <vector>
using namespace std;
 vector <double> phi(vector <double> Array, int c, int n){
   int a = 0;
   int b = 0;
   int d = 0;
   for (int i = c; i < n; i++)
    if (Array[c] < Array[i])
     b++;
   vector <double> Array1(b);
   vector <double> Array2(n - b);
   for (int i = c; i < n; i++)
    if (Array[c] < Array[i]){
     Array1[c + a] = Array[i];
     a++;
    }
   for (int i = c; i < n; i++)
    if (Array[c] > Array[i]){
     Array1[b + d] = Array[i];
     d++;
    }
   return phi(Array1, 0, b), Array[c], phi(Array2, b, n);
 }
 int main(){
   int n;
   int b = 0;
   cin >> n;
   vector <double> Array(n);
   for (int i = 0; i < n; i++)
    cin >> Array[i];
   Array = phi(Array, 0, n);
   for (int i = 0; i < n; i++)
    cout << Array[i] << " ";
   return 0;
 }
